package org.runway.web.utils;

public class URIUtils {
	
/*	public static String getGroupNameFromUri( String uri){
		String groupName = null;
		
		if( uri.startsWith("/g/group")){
			String[] g =uri.split("/g/group/");
			if( g!= null && g.length==2){
				String[] grp = g[1].split("/");
				if( grp != null && grp.length ==2){
					groupName = grp[0];
				}
			}
		}
		
		return groupName;
	}
*/
}
